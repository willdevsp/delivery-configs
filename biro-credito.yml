eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring: 
  jpa:
    hibernate:
      ddl-auto: create
    datasource:
      url: mysql://localhost:3306/db
      username: usuario
      password: usuario
